import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 24,
  },
  confirmContainer: {
    flexDirection: "row",
    height: 200,
    justifyContent: "center",
    alignItems: "center",
  },
  confirmText: {
    fontSize: 30,
    textAlign: "center",
  },
});
