import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 24,
    marginBottom: 60,
  },
  item: {
    backgroundColor: "#f9c2ff",
    padding: 20,
    marginVertical: 12,
    // marginHorizontal: 16,
  },
});
